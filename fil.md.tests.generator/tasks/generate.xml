<?xml version="1.0" encoding="UTF-8"?>
<project default="generate" name="fil.md.tests.generator">
    <property name="ECLIPSE_HOME" value="../../../../../../Programmes/eclipse/modeling-2021-09/eclipse"/>
    <property name="ECLIPSE_WORKSPACE" value="../../../../runtime-EclipseApplication"/>

    <!-- The classpath with only the dependencies used by the project -->
    <path id="fil.md.tests.generator.libraryclasspath">
        <pathelement location="${ECLIPSE_WORKSPACE}/fil.md.tests.generator/bin"/>
    </path>

    <path id="fil.md.tests.generator.classpath">
        <path refid="fil.md.tests.generator.libraryclasspath"/>
    </path>

    <!-- The classpath with only the jars available in the plugin folder of Eclipse -->
    <path id="fil.md.tests.generator.libraryclasspath.alljars">
        <pathelement location="../fil.md.tests.generator/bin"/>
        <fileset dir="${ECLIPSE_HOME}/plugins/" includes="*.jar"/>
    </path>

    <path id="fil.md.tests.generator.classpath.alljars">
        <path refid="fil.md.tests.generator.libraryclasspath.alljars"/>
    </path>

    <!-- The default task with the jars used by this project -->
    <target name="generate">
        <java classname="fil.md.tests.generator.main.Generate" classpathref="fil.md.tests.generator.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>

    <!--
        In case of problem you can also try with all the jar available in your Eclipse
        Keep in mind that having two versions of the same jar can create problems
    -->
    <target name="generate.alljars">
        <java classname="fil.md.tests.generator.main.Generate" classpathref="fil.md.tests.generator.classpath">
            <arg value="${model}"/>
            <arg value="${target}"/>
        </java>
    </target>
</project>
